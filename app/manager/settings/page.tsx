
'use client'
import {DashboardShell} from '@/components/dashboard-shell';import {useState} from 'react'
export default function Settings(){const [msg,setMsg]=useState('');const items=[{href:'/manager/dashboard',label:'Overview'},{href:'/manager/settings',label:'Settings'}];return <DashboardShell title="Editable Settings" role="Manager / Owner" items={items}><div className="card"><div className="formgrid"><div className="field"><label>Free shipping threshold</label><input defaultValue="69.99"/></div><div className="field"><label>Cash App number</label><input defaultValue="6466588061"/></div><div className="field"><label>Same-day service</label><select defaultValue="on"><option value="on">Enabled</option><option value="off">Disabled</option></select></div><div className="field"><label>Website status</label><select defaultValue="live"><option value="live">Live</option><option value="maintenance">Maintenance</option></select></div></div><button className="glowbtn" style={{marginTop:18}} onClick={()=>setMsg('Settings saved')}>Save Settings</button>{msg&&<p className="badge" style={{marginLeft:12}}>{msg}</p>}</div></DashboardShell>}
